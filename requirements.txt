# Core neural TTS
TTS==0.22.0

# Deep learning backend
torch>=2.0.0

# Audio processing
soundfile>=0.12.1
librosa==0.10.0

# Build & compatibility (important on Windows)
Cython>=0.29
numpy<2.0

# Optional (only if you export MP3 later)
# pydub
